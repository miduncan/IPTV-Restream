# Optional shared gate for the entire site. Leave both empty to disable it.
# Use HTTPS in public deployments because HTTP Basic Auth credentials are not encrypted.
BASIC_AUTH_USERNAME=friends
BASIC_AUTH_PASSWORD=replace-with-a-long-random-passphrase
